# These tests make sure taht all operations work with a vsi dataset.
# Ideally future tests of functions should always include a vsi example

vrt_filename <- file.path(get_data_path(),"CHELSA_2.1_bio_vsi.vrt")
