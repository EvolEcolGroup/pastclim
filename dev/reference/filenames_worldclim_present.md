# Generate file names to download the WorldClim present dataset

This function creates a vector of paths needed to download the WorldClim
present dataset

## Usage

``` r
filenames_worldclim_present(dataset, bio_var)
```

## Arguments

- dataset:

  the name of the dataset of interest

- bio_var:

  the variable of interest

## Value

a vector of times, one per band
